\documentclass[crop,tikz]{standalone}

\input{includes.tex}

\begin{document}

\begin{tikzpicture}[node distance=0.8cm and 1.1cm, scale=0.6, transform shape]

  % Root node
  \node (root) [root, text width=2cm, align=center] {Root Node\\All Data\\Type: Empty};

  % First level splits
  \node (b) [decision, below left=0.8cm and 0.3cm of root] {$x_1 \leq 0.7$};
  \node (e) [rpfuniquedecision, below right=0.8cm and 0.3cm of root] {$x_2 \leq 0.5$};

  % Connections from root
  \draw [arrow] (root) -- (b);
  \draw [arrow] (root) -- (e);

  % % Nodes from first x1 split
  \node (c) [rpfunique, below left=1cm and 0.7cm of b, text width=1.7cm, align=center] {Node 2\\Type: $x_1$};
  \node (d) [internalnode, below right=1cm and 0.7cm of b, text width=1.7cm, align=center] {Node 3\\Type: $x_1$};

  % % Connections from first split
  \draw [arrow] (b) -- node[label, left] {Yes} (c);
  \draw [arrow] (b) -- node[label, right] {No} (d);

  % % Further splits from Node 2
  \node (h) [decision, below left=1cm and 0cm of c] {$x_1 \leq 0.4$};
  \node (k) [rpfuniquedecision, below right=1cm and 0cm of c] {$x_3 \leq 0.3$};

  % % Connections from Node 2
  \draw [arrow] (c) -- (h);
  \draw [arrow] (c) -- (k);

  % % Nodes from x1 <= 0.4 split
  \node (i) [internalnode, below left=1cm and 0cm of h, text width=1.9cm, align=center] {Node 4\\Type: $x_1$};
  \node (j) [internalnode, below right=1cm and 0cm of h, text width=1.9cm, align=center] {Node 5\\Type: $x_1$};

  % Connections from x1 <= 0.4 split
  \draw [arrow] (h) -- node[label, left] {Yes} (i);
  \draw [arrow] (h) -- node[label, right] {No} (j);
\end{tikzpicture}
\end{document}
